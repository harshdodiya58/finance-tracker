// Clean placeholder App module. `main.jsx` currently imports `AppClean.jsx` so this
// file is kept minimal to avoid any syntax issues while we repair the project.
export default function App() {
  return null;
}
import { AuthProvider, useAuth } from './context/AuthContext';
